server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # NOTE CSP : la Content-Security-Policy n'est PAS définie ici. Le proxy
    # public (infra/nginx) est la couche canonique pour les en-têtes de
    # sécurité — il masque d'ailleurs ceux de ce conteneur
    # (proxy_hide_header). En définir une ici enverrait DEUX en-têtes CSP au
    # navigateur, qui applique alors leur intersection : c'est ce qui avait
    # cassé l'analytics en production. Une seule source de vérité : le edge.


    # Security headers (re-declared per location because nginx add_header does
    # NOT inherit into a child block that defines its own add_header directives).
    # ── Content-hashed build assets (filename changes on every rebuild) ─────────
    # Safe to cache aggressively: a new deploy ships new filenames.
    location /assets/ {
        add_header X-Frame-Options DENY always;
        add_header X-Content-Type-Options nosniff always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        try_files $uri =404;
    }

    # ── index.html must always be revalidated ──────────────────────────────────
    # Without this, browsers apply heuristic caching and keep serving a stale
    # bundle after a deploy, so shipped fixes never reach users.
    location = /index.html {
        add_header X-Frame-Options DENY always;
        add_header X-Content-Type-Options nosniff always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    # ── SPA fallback ───────────────────────────────────────────────────────────
    location / {
        add_header X-Frame-Options DENY always;
        add_header X-Content-Type-Options nosniff always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header Cache-Control "no-cache" always;
        try_files $uri $uri/ /index.html;
    }
}
